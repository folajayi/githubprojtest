package org.amazon.pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

import java.util.List;

public class SearchResultPage {

    /*public SearchResultPage(WebDriver driver) {
    }

    public class SearchResultsPage {
        private WebDriver driver;

        private By searchResultLocator = By.cssSelector(".s-search-results .s-result-item");

        public SearchResultsPage(WebDriver driver) {
            this.driver = driver;
        }

        public void selectFirstMonitor() {
            List<WebElement> searchResults = driver.findElements(searchResultLocator);
            if (!searchResults.isEmpty()) {
                searchResults.get(0).click();
            } else {
                throw new RuntimeException("No search results found");
            }
        }
    }*/

}

